export type Breakpoints = string[]

export type MediaQueries = {
  xs: string
  sm: string
  md: string
  lg: string
  xl: string
}

export type Colors = {
  primary: string
  secondary: string
  border: string
  textPrimary: string
  textSecondary: string
}
